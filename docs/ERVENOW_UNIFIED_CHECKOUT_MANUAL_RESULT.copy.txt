ERVENOW — نتيجة الاختبار اليدوي لـ Unified Checkout
التاريخ: 2026-09-22
النوع: توثيق فقط. الاختبار اليدوي هو المرجع. لا Features. لا تعديل كود.
PUBLIC_ORDERING: لم يُفعَّل في .env ولا في data/public-ordering.json.

البيئة: localhost:4000 — العملية المحلية رفعت PUBLIC_ORDERING في الذاكرة فقط. القفل في الملف false.

خط الأساس السابق: orders=47 · notifications=168 · ledger=135
بعد الاختبار: orders=49 · notifications=172 · ledger=135

========== الحساب ==========
Customer ID: 8578cc4c-6b63-4814-9dd8-31a6ef2ae007
الجوال: 966530792754
الدور: store (حساب موجود منذ 2026-05-10 — ليس Customer جديدًا)
OTP: واتساب otp_login إلى 9665***754 ثم verify-otp 200 قبل كل إنشاء ناجح
رقم موجود رُبط بالحساب الحالي. مسار رقم جديد → Customer تلقائي لم يظهر.

========== الطلبات ==========
ED-22-653 bc452287-…2a3e ~12:57 — كبسار 1fcc6d83-… — restaurant / merchant — 51.75 (45+ضريبة) — استلام — نقد معلّق
ED-22-598 795862a9-…9775 ~13:20 — كبسار نفسه — restaurant / merchant — 27.60 (24+ضريبة) — استلام — نقد معلّق

customer_id في الطلبين = المعرّف أعلاه
merchant_id = ad3c8458-… (مالك كبسار، ليس جلسة المختبِر)
لا Order هجينة. لا نقل/خدمة. لا سلة مختلطة كأكثر من Order في طلب واحد (ordersCount=1 مرتين).

محاولة أولى: order/create 400 بعد OTP بدون INSERT. ثم قفزة العدّاد فقط مع 200.

========== Fulfillment Groups ==========
مُثبت: مجموعة مطعم واحدة store:كبسار → Order واحدة لكل تأكيد.
غير مُثبت: متجران / مطعم+صيدلية+سطحة كعدة Orders.

========== /my-orders ==========
بعد كل إنشاء ناجح: GET /my-orders ثم GET /api/order/orders = 200 لنفس customer_id. القائمة تُفلتر بـ customer_id حتى لو role=store. حجم الاستجابة كبر بعد الطلب الثاني.

========== الجهة الصحيحة ==========
تاجر كبسار: store_id + portal_type=merchant + إشعار merchant.order.new إلى ad3c8458-…
الإدارة: إشعار «طلب متجر جديد» لكل رقم
نقل/خدمة: لا طلبات

========== Notifications ==========
+4 بعد الإنشاء فقط (168→172). لكل Order: متجر + أدمن. لا إشعار عميل لهذين الطلبين. واتساب في السجل = OTP فقط.

========== Ledger ==========
بقي 135. آخر حركة 2026-09-20. متوقع لأن الدفع نقد معلّق. Redis المحلي كان ECONNREFUSED؛ الإنشاء اكتمل بدونه.

========== لا Order قبل OTP ==========
verify-otp ثم order/create. الـ 400 بعد OTP لم يُدخل صفًا. 47→49 فقط مع 200.

========== هل رحلة العميل جاهزة تقنيًا لتفعيل PUBLIC_ORDERING؟ ==========
لا — ليس لتفعيل الإنتاج / .env.

ثبت: مطعم واحد → OTP لرقم موجود → Order واحدة بـ customer_id صحيح → /my-orders → تاجر كبسار + أدمن → لا Ledger نقدًا → لا INSERT قبل OTP.

لم يثبت: عميل جديد؛ مجموعات متعددة؛ كتالوج غير المطعم؛ EW Pay/دفتر؛ Redis/طابور؛ Dispatch/Settlement/Actions.

.env و data/public-ordering.json ما زالا false.
